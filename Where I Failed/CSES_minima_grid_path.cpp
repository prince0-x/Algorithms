// special thanks to @ erichtto + some kinda special hashing technique clled => coordinate compression

#include<bits/stdc++.h>
using namespace std;
string solve(vector<string>&grid, int n){

}
int main(){
      int n;
      cin>> n;
      vector<string>grid(n);
      for(int i=0;i<n;i++){
            cin>>grid[i];
      } 
      cout<<solve(grid, n)<<"\n";
      return 0;
}